import {type SchemaTree} from 'src/tree/tree.class.js';
import {SchemaUriNode} from 'src/tree/types.js';
import {type RenderManifest} from './types.js';

export function plotForDocusaurus(tree: SchemaTree): RenderManifest {
	return [];
}
